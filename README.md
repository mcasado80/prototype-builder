# prototype-builder

Repository for unattended prototype builds via Cursor Background Agents. Each prototype lives on its own branch named prototype/<slug>. Vercel auto-deploys a preview URL for every branch push. See AGENTS.md for agent behavior.
